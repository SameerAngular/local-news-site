import { defineConfig } from "astro/config";

export default defineConfig({
  site: "https://your-domain.com", // TODO: replace with your real domain
  // Astro output is static by default — perfect for free hosting on Netlify/Vercel
});
